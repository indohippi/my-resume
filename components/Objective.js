import styles from '/styles/Objective.module.css';

const Objective = () => (
  <section className={styles.objectiveSection}>
    <h2 className={styles.objectiveTitle}>Objective:</h2>
    <p className={styles.objectiveText}>
      Aiming to leverage extensive experience in web development and data analysis
      in a challenging software or web developer role.
    </p>
  </section>
);

export default Objective;
